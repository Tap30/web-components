export const ErrorMessages = {
  INVALID_DISCOUNT: "Invalid discount code",
  EXPIRED_DISCOUNT: "Discount code has expired",
} as const;

export const Slots = {
  DEFAULT: "default",
  DISCOUNT_CODE: "discount-code",
  APPLY_BUTTON: "apply-button",
} as const;
