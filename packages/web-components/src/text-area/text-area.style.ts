import { css } from "lit";

export default css`
  .control {
    height: 6.5rem;
    padding: var(--tapsi-spacing-5) var(--tapsi-spacing-6);

    align-items: flex-start;
  }

  .input {
    resize: none;
  }
`;
