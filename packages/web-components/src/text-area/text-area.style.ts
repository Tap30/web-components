import { css } from "lit";

export default css`
  .control {
    height: 6.5rem;
    padding: var(--tap-sys-spacing-5) var(--tap-sys-spacing-6);

    align-items: flex-start;
  }

  .input {
    resize: none;
  }
`;
