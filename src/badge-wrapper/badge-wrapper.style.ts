import { css } from 'lit';

export default css`
  .wrapper {
    position: relative;
    display: inline-flex;
  }

  .badge {
    display: inline-flex;
    position: absolute;
    top: 0;
    left: 0;
    transform: translate(-50%, -30%);
  }
`;
